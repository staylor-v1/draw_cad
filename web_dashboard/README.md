# Draw CAD Development Dashboard

Run the local dashboard with:

```bash
python web_ui.py --port 12080
```

The first implementation uses a mock Gemma 4 analysis response shaped like the
future model output. Uploaded drawings are stored under `experiments/web_dashboard/`,
which is already ignored by git.
