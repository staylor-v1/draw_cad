from build123d import *

# Conservative baseline generated by the roundtrip harness: fallback from raster CONNECTING-ROD-1024x724.webp.
# Gemma did not produce an executable model, so use the drawing envelope.
part = Box(102.4, 72.4, 8.688)
